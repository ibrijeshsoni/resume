services:
  resume-site:
    build: .
    container_name: brijeshsoni-resume
    ports:
      - "10110:80"
      - "10443:443"
    restart: always
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
